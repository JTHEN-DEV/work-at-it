import React from "react";

type Props = {};

const LandingPage = (props: Props) => {
    return (
        <div>
            <h1>This is the landing page advertising the stuff</h1>
        </div>
    );
};

export default LandingPage;
